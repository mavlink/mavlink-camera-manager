# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# NOTE: frontend/bindings/ is NOT gitignored. The generated TypeScript bindings
# are committed so CI can detect unintended API changes. See .githooks/pre-push
# and the "Generate and check bindings" step in .github/workflows/rust.yml.
